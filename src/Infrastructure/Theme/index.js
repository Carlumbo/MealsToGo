import { colors } from './colors';
import { space, lineHeights } from './spacing';
import { sizes, sizes2 } from './sizes';
import { fonts, fontWeights, fontSizes } from './fonts';

export const theme = {
  colors,
  space,
  lineHeights,
  sizes,
  sizes2,
  fonts,
  fontSizes,
  fontWeights,
};
